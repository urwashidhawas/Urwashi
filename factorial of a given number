def factorial(a):
	if a== 1 or a==0:
		return 1
	else:
		return a*factorial(a-1)
a= int(input("Enter a number : "))
if a>=0:
	 print(f"Factorial of given number is : {factorial(a)}\n")
else:
	print("Enter a positive number\n")
